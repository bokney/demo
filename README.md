# demo

it makes a gameboy rom

install dependencies:

```sh
make clone_unity
make download_gbdk
```

run build steps individually:

```sh
make run_tests
make compile
make assemble
make link
make clean
```
